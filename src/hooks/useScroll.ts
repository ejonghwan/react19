import { useEffect } from "react"

const useScroll = ({ el }) => {

    const handler = (e: React.MouseEvent<React.ReactNode>) => {

    }

    useEffect(() => {

    }, [])


    return { el, handler }
}

export default useScroll